apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "payment-platform.fullname" . }}-api
  labels:
    {{- include "payment-platform.apiLabels" . | nindent 4 }}
spec:
  {{- if not .Values.api.hpa.enabled }}
  replicas: {{ .Values.api.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "payment-platform.apiSelectorLabels" . | nindent 6 }}
  strategy:
    type: {{ .Values.api.strategy.type }}
    {{- if eq .Values.api.strategy.type "RollingUpdate" }}
    rollingUpdate:
      maxSurge: {{ .Values.api.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.api.strategy.rollingUpdate.maxUnavailable }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "payment-platform.apiSelectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: api
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.api.service.targetPort }}
              protocol: TCP
          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: {{ include "payment-platform.configmapName" . }}
                  key: APP_ENV
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "payment-platform.configmapName" . }}
                  key: LOG_LEVEL
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "payment-platform.configmapName" . }}
                  key: DB_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: {{ include "payment-platform.configmapName" . }}
                  key: DB_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "payment-platform.fullname" . }}-db-credentials
                  key: DB_PASSWORD
            - name: PAYMENT_WORKER_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "payment-platform.configmapName" . }}
                  key: PAYMENT_WORKER_BASE_URL
          livenessProbe:
            httpGet:
              path: {{ .Values.api.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.api.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.api.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.probes.readiness.periodSeconds }}
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
          {{- if .Values.secretsStore.enabled }}
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
          {{- end }}
      {{- if .Values.secretsStore.enabled }}
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "payment-platform.secretProviderClassName" . }}
      {{- end }}
