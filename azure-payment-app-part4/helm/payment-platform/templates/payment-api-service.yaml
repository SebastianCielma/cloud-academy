apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-platform.fullname" . }}-api
  labels:
    {{- include "payment-platform.apiLabels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "payment-platform.apiSelectorLabels" . | nindent 4 }}
