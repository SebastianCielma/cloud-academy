network:
  resource_group_name: terraops-prod-rg
  location: westeurope
  vnet_cidr: "10.32.0.0/16"
  subnet_cidr: "10.32.1.0/24"

application:
  name: academy-control-api
  version: "1.3.0"
  service_path: services/academy-control-api

runtime:
  environment: prod
  port: 8000
  host: "0.0.0.0"

database:
  engine: sqlite
  path: /opt/academy-control-api/prod.sqlite

vm:
  size: Standard_B1s
  admin_username: azureuser
